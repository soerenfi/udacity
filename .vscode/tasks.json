{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build OSM",
            "type": "shell",
            "command": "cmake -DCMAKE_BUILD_TYPE=Debug .. &&  make -j",
            "options": {
                "cwd": "${workspaceFolder}/CppND-Route-Planning-Project/build"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}